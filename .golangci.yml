run:
  deadline: 13m
  modules-download-mode: vendor

linters-settings:
  dupl:
    threshold: 100
  gocyclo:
    min-complexity: 50
  govet:
    enable:
      - nilness

linters:
  enable:
    - golint
  disable-all: true

